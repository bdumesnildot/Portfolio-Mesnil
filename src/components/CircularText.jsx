import "../styles/components-styles/CircularText.scss"

const CircularText = ({ text, radius, fontSize }) => {

  return (
    <div className='circular-text'>
      circular text
    </div>
  );
};

export default CircularText;